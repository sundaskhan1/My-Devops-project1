name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Verify Project Files
      run: |
        echo "Starting build pipeline..."
        ls -la
        echo "HTML and CSS files verified successfully!"